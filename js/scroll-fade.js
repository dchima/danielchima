
/*var scrollFade = function(){
  var div = $(".fader:not(.animated)");
  var b = $(window).scrollTop() + $(window).height();

  div.each(function (x){
    var a = $(this).offset().top + $(this).height();

    if(a < b){
      $this.addClass(".animated").addClass(".fadeInUp");
    }
  });
};

$(scrollFade);
$(window).scroll(scrollFade);*/
